@using SchedulerWebApp.Models.ViewModels
@model LoginViewModel
@{
    ViewBag.Title = "Log in";
}

<h2>@ViewBag.Title.</h2>

<style type="text/css">
    .img-responsive {
        margin: 0 auto;
    }
</style>

<div class="row">
    <div class="col-md-8">
        <section>

            <!-- #region Modal used for log in -->
            @*<div id="gifModal" class="modal fade" tabindex="-1" role="dialog" data-backdrop="static">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-body">
                                <img alt="Loading..." class="img-responsive" src="@Url.Content("~/Images/loading2.gif")" />
                            </div>
                        </div><!-- /.modal-content -->
                    </div><!-- /.modal-dialog -->
                </div><!-- /.modal -->*@
            <!-- #endregion -->
            @using (Html.BeginForm("Login", "Account", new { ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", id = "loginForm" }))
            {
                @Html.AntiForgeryToken()
                <h4>Use a local account to log in.</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.Email, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Password, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        <div class="input-group">
                            @Html.PasswordFor(m => m.Password, new { id = "password", @class = "form-control" })
                            <div id="passwordIcon" class="input-group-addon"><i class="glyphicon glyphicon-eye-open"></i></div>
                        </div>
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="checkbox">
                    <div class="col-md-offset-4 col-md-8">
                        <label>
                            @Html.CheckBoxFor(m => m.RememberMe)
                            @Html.LabelFor(m => m.RememberMe)
                        </label>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-4 col-md-8">
                        <button id="submitBtn" type="submit" class=" btn btn-primary btn-block">
                            log in
                        </button>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-4 col-md-8">
                        @Html.ActionLink("Register as a new user", "Register", "Account", null, new { @class = "btn btn-info btn-block" })
                    </div>
                </div>


                @* Enable this once you have account confirmation enabled for password reset functionality
                    <p>
                        @Html.ActionLink("Forgot your password?", "ForgotPassword")
                    </p>*@
            }
        </section>
    </div>
    <div class="col-md-4">
        <section id="socialLoginForm">
            @Html.Partial("_ExternalLoginsListPartial", new ExternalLoginListViewModel { ReturnUrl = ViewBag.ReturnUrl })
        </section>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/custom")

    <script type="text/javascript">
        showHidePassword('#passwordIcon', '#password');

        $('form').submit(function (e) {
            //$('#gifModal').modal('show');

            var isValid = $(this).valid();
            console.log(isValid);

            if (isValid) {
                changeButtonText('submitBtn', 'logging in');
            }
        });
    </script>
}